diff --git a/cfg/pgrapher/experiment/protodunevd/pdvd_track_fitting.json b/cfg/pgrapher/experiment/protodunevd/pdvd_track_fitting.json
index 64f2cf42..f8f9f595 100644
--- a/cfg/pgrapher/experiment/protodunevd/pdvd_track_fitting.json
+++ b/cfg/pgrapher/experiment/protodunevd/pdvd_track_fitting.json
@@ -59,5 +59,8 @@
     "_comment_proj_skip_unmapped_face": "doc pdvd/45 sec 13 (2026-09-05): do_single_tracking's 2nd-pass projection loop dereferenced a MISSING wpid_offsets/wpid_slopes entry when IDetectorVolumes::contained_by() names an (apa, face) absent from this event's geometry table (PDVD apa 2 face 0 at y ~ 1 cm: gap-bridging path points), writing run-to-run garbage into pu/pv/pw/pt. 1 = skip such points (trajectory_fit's loop already does). Fires on 2 of the 120 STM-set events; every production product (mabc-pr.zip, calib dump, tracking-pr.root) identical 120/120 on both chains; only the STM fit dump of 039253/16 changes and becomes deterministic. C++ default 0 = the legacy UB path.",
     "proj_skip_unmapped_face": 1.0,
     "_comment_traj_final_fill_charge_test": "doc pdhd/11, owner flip 2026-09-07 (line refs corrected doc pdvd/61 sec 9 item 1 -- ~12 lines drifted since, and the gap threshold below was stated wrong: the FINAL organize_ps_path's low_dis_limit argument is do_single_tracking's own pass-2-halved local, 0.6 cm at the shipped 12.0 mm default, not the 1.2 cm config default -- so its 'gaps >= 1.6 * low_dis_limit' fill threshold is 1.6 * 0.6 cm = 0.96 cm, not 1.92 cm).  Applies the form_map charge keep-rule to the points that FINAL organize_ps_path(low_dis_limit/2., 0) inserts (TrackFitting.cxx, do_single_tracking's third organize call).  Before this, that last straight-line middle fill was the only stage that moved fit points with NO charge test after it -- there is no form_map, examine_point_association or skip_trajectory_point between it and the PR::Fit construction loop -- so points form_map had already rejected came back and were written out.  C++ default 0 = OFF; this key is what turns it on, and it is set in pdhd_track_fitting.json and pdvd_track_fitting.json ONLY -- sbnd_track_fitting.json and the uBooNE presets are untouched, and TrackFitting.cxx is shared, so the absent key is what keeps them still.  MEASURED, PDHD 028084 31 events, matched 537-block population: blocks with median fit->charge > 3 cm 49 -> 15, bad fitted points 15624 -> 6161, accepted (status 0) 43.9 % -> 11.7 % beyond 3 cm; it drops 18 blocks of 690 and all 18 were already bad.  PDVD 039252 evts 5+16: 1 bad block -> 0, points beyond 10 cm 0.020 -> 0.006, and NO blocks lost (41 both sides).  WHAT IT CHANGES: fitted trajectories, so T_rec_charge, the stm_fit Bee layer and STM verdicts all move -- on PDHD without unmerge_assoc it shifts 34 STM tags in and 4 out; with unmerge_assoc on it moves 1 in and 1 out.  NOT bit-identical, by design.  See doc pdhd/11 sec 10.2-10.3.",
-    "traj_final_fill_charge_test": 1
+    "traj_final_fill_charge_test": 1,
+    "_comment_d103_fit_lattice_knobs": "doc pdvd/103 sec 14, FLIPPED for PDVD on the owner's go 2026-09-15. The two doc pdvd/101 track-fit lattice knobs. fit_weight_pow 1.5 replaces the fit's q^2 point weight by q^1.5; assoc_cont_center 1 centres the association window on the cell instead of rounding it. Graded on the PDVD production lineage together with the retile charge_stepped flip (arms d103v0 -> d103v1, truth = doc 100's carried record under the owner adjudications own103v and own103v2): is_stm purity 0.980 -> 0.982, efficiency 0.612 -> 0.679; michel purity 0.924 -> 0.940, efficiency 0.661 -> 0.715; D1 under figs/103_pred.txt and its amendment 6. C++ defaults are 2.0 and 0, so REMOVING these two keys restores the pre-flip fit exactly. pdhd_track_fitting.json does NOT carry them: PDHD fails Michel purity under the same levers (doc 103 sec 12.3) and is not flipped. Read at RUNTIME: a byte-identical compiled jsonnet does NOT mean the fit is unchanged.",
+    "fit_weight_pow": 1.5,
+    "assoc_cont_center": 1
 }
