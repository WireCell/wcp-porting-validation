# doc pdvd/39 round 3 -- the STM Bee layers move to the VERDICT population (owner decision 2026-09-05)
# 2026-09-05
#
# HEADs at the run:  toolkit b72d00e0   wcp-porting-img 59500a50
# Pin (BOTH sides -- the binary is identical, only the compiled jsonnet differs):
#   /home/xqian/tmp/d44_libpin/new   libWireCellClus.so md5 fd273dc8f000780f   libWireCellRoot.so 48e63189e8c192f5
# Pctree: 039252_2_d39r2prov, 039349_23_d41prov (via scripts/stage_pr_tag.sh, fresh tags, M13)
#
# CODE CHANGE: none.  cfg/pgrapher/experiment/protodunevd/pr.jsonnet, bee_points_sets only:
#   stm_fit             (no gate)                -> require_flag:'STM'
#   stm                 require_pc:'stm_fit'     -> require_flag:'STM'
#   steiner_graph       require_pc:'stm_fit'     -> require_flag:'STM'
#   steiner_terminals   require_pc:'stm_fit'     -> require_flag:'STM'
#   stm_tagged          require_flag:'STM'       -> REMOVED (drew the same object set as 'stm')
# require_flag is doc 39 round 2's existing field (MultiAlgBlobClustering.cxx:863), applied in the
# per-cluster loop that feeds fill_bee_points_from_cluster, so it gates the pcname=="stm_fit" branch
# exactly as it gates the '3d' and steiner_pc branches.  NOT ANDed with require_pc on purpose: a
# cluster can be evaluated with no pass recorded (TaggerCheckSTM.cxx:609), and require_pc would then
# hide it even if tagged.
#
# ARMS
#   reference  work/039252_2_d44prod, work/039349_23_d44prod       (doc 44 gate 5, canonical JSON, no TLAs)
#   round 3    work/039252_2_d44beestm, work/039349_23_d44beestm   (same, with the rescoped jsonnet)
#              ARM=d44beestm PIN=new EVENTS=<2-event file> JOBS=4 ./docs/nf_sp_img_clus/scripts/run_d44_arms.sh
#   -nu pair   work/039252_2_d44nuchk (before) vs work/039252_2_d44beenu (after), ./run_pr_evt.sh -s <tag> -nu 39252 2
#
# GATE 1  COMPILED-CONFIG PROOF.  .wct-pr_d44prod.json vs .wct-pr_d44beestm.json, arm names normalized:
#   the ENTIRE compiled wct-pr-perevt.jsonnet differs in exactly one place -- bee_points_sets 9 -> 8 entries,
#   with the five changes above and nothing else.  No pipeline node, no other component's data, no path moves.
#   (Re-verified after a comment-only edit: a fresh wcsonnet with the same TLAs == the config the arm ran.)
#
# GATE 2  NOTHING OUTSIDE THE BEE DISPLAY MOVES.
#   tracking-stm.root   hash_root_trees.py --per-tree   SAME both events
#                       file 216b3031e2328eed...  T_rec_charge 3d0cc3da8eca2304...
#   mabc-pr.zip members, content sha256 (both events, -stm -stm-fit):
#       22 -> 21 members; stm_tagged REMOVED; 17 of the 21 IDENTICAL (clustering-global + all 16 dead-area);
#       exactly 4 DIFFER: stm, stm_fit, steiner_graph, steiner_terminals -- the rescoped layers.
#   -nu chain (039252/2): 26 -> 25 members; 21 IDENTICAL incl. track_fit, vertices, shower_track, mc;
#       the same 4 differ; 0 error/exception lines in the log.
#   d42_proj2d_selfcheck.py on 039252_2_d44beestm: PASS (every fitted block predicts on its own footprint).
#   => clustering-global byte-identical is the load-bearing row: tagger, fit and clustering all ran unchanged.
#
# GATE 3  stm_tagged LOSES NOTHING.  Round-2 0-stm_tagged-global.json vs round-3 0-stm-global.json:
#   every array agrees -- x, y, z, q, cluster_id, real_cluster_id -- and the runNo/subRunNo/eventNo/geom
#   header; the ONLY difference is the layer's own 'type' string ("stm_tagged-global" vs "stm-global").
#   Checked on 039252/2 (4902 pts / 8 cl) and 039349/23 (3577 pts / 3 cl).
#
# WHAT THE DISPLAY NOW SHOWS (round 2 -> round 3)
#                        039252/2 (evt 298595)          039349/23 (evt 19869)
#   stm                  53659 pts / 16 cl -> 4902 / 8   16102 / 16 -> 3577 / 3
#   stm_fit               4342 / 16 -> 1000 / 8           4362 / 16 ->  952 / 3
#   steiner_graph        49217 / 16 -> 3032 / 8          14407 / 16 -> 3029 / 3
#   steiner_terminals     9217 / 16 ->  728 / 8           3657 / 16 ->  773 / 3
#   298595 tagged {39,55,79,83,86,103,109,113} is a subset of fitted
#          {38,39,45,55,79,83,84,86,87,103,105,108,109,110,113,119}: all four layers agree object-for-object.
#
# COST (recorded, not hidden): the fitted-but-REJECTED clusters (8 on 298595, 13 on 19869) are no longer
#   drawn on any layer.  "Why was this one not tagged?" now needs the log's STM=0 lines or a one-off arm
#   restoring require_pc:'stm_fit'.  Round 2 added those layers for that question (doc 39 sec 11); the
#   owner chose this trade knowingly.
#
# EPOCH NOTE (checked, not assumed).  doc 39 sec 16.3 reports 5 STM tags on 298595 under unmerge_assoc
#   (2026-09-04).  These arms DO run unmerge_assoc (99 log lines; 523 clusters in the live grouping, the
#   unmerged count sec 16.3 measured as 522) and the log shows 54 clusters evaluated, 8 at STM=1.  The
#   difference is the two production flips in between -- curved_fv p90+5 cm (doc 41/43, 09:16) and the
#   effective transverse smearing (doc 44 sec 7.1) -- both of which move STM verdicts.  sec 16.3's tag set
#   is the 2026-09-04 configuration; the counts in this file are today's.
#
# BEE-URL TRAP: a Bee layer route returns HTTP 200 for a layer that is NOT in the zip, with the body
#   "<path>/0-<name>-global.json does not exist" -- byte-for-byte the same shape a name that never existed
#   returns.  So a status-code probe cannot confirm a REMOVED layer.  Use the set's event/list/ page, which
#   enumerates only the layers present: the round-3 set lists nine, without stm_tagged.
#
# BEE
#   round 3, evt 298595 (-nu chain, canonical post-flip config):
#     https://www.phy.bnl.gov/twister/bee/set/971dc70d-98ca-4f30-88e7-76078ccf64dc/event/list/
#   round 2 / doc 44 production, same event, for comparison:
#     https://www.phy.bnl.gov/twister/bee/set/3106aeb0-3a07-41c5-881e-99ba5cd332e0/event/list/
