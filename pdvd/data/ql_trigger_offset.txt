# run  trigger_offset_us  (per-run RESIDUAL on top of the per-event metadata)
#
# EMPTY ON PURPOSE (2026-07-10): the light<->charge offsets are now measured
# PER EVENT and PER CRATE from the rawwf trigoff/trigger_offset tree and
# stamped into the opflash archive metadata (offset_bot_us = BDE/bottom,
# offset_top_us = TDE/top) by run_light_evt.sh -- the charge windows float
# +-15 us event-to-event and the two crates open up to 32 us apart, so no
# per-run constant can replace them.  This table remains ONLY for a residual
# constant on top of the metadata values (applied to BOTH crates by
# run_clus_evt.sh), should the physics closure ever show one.
# NOTE: do not confuse with data/jjo_triglight_offsets.txt (jjo's per-event
# timestamp table, fs_t0-referenced, different sign/anchor conventions).
# History + closure: pdvd/docs/pdvd-ql-pending.md sec 1,
# ql_light_calib/fit_trigger_offset.py docstring.
#
# Format: "<run> <offset_us>" per line, e.g.
# 039252 -250.0
