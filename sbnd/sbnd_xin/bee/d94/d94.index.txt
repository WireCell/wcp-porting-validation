# doc 94 -- vertex_hadron_guard A/B.  9 events, SAME ORDER in both sets, so
# bee_idx 0..7 still line up 1:1 with the colleague's set
# 9797078d-763a-4202-9af0-2d53127f1bd2; idx 8 is the one data bundle the guard
# releases out of 3067 events.
#
# OFF (production, prod0901b)  https://www.phy.bnl.gov/twister/bee/set/fba260db-6834-46fa-aa95-2a759ce29269/event/list/
# ON  (stm_vertex_hadron_guard) https://www.phy.bnl.gov/twister/bee/set/6bf6dbc6-57b9-4f6b-80e4-5dbe8ef412d8/event/list/
# (superseded: abc72dc0-a4c4-40aa-8bc2-6f8bc5ba65fc -- 8 events, OFF only)
#
# WHY MOST EVENTS CARRY NO PR LAYERS IN THE **OFF** SET, AND THAT IS THE POINT:
# TaggerCheckNeutrino refuses a bundle whose main is cosmic-tagged
#   "[nu_per_bundle] gid 10 activity 6 (L 93.9 cm) cosmic-tagged (STM=true);
#    not a candidate"  -> "no main cluster selected ...; skipping"
# so no PR graph is built, and MultiAlgBlobClustering then leaves the
# track_fit / shower_track / vertices point sets EMPTY BY DESIGN
#   "visitor TaggerCheckNeutrino:pr produced no PR graph; leaving the set
#    empty (require_pr_graph)"
# and writes no calib dump.  An STM tag therefore costs the whole PR
# reconstruction, not just a label.  Compare idx 2/3/5/8 across the two sets:
# the layers APPEAR once the guard releases the event.
#
# Layers: img-global, clustering-global (Q/L), clustering-pr-global (post-PR
# in-scope), op, deadarea, stm_fit-global (the fitted STM path + per-point
# dQ/dx), and -- only where a nu candidate exists -- track_fit-global,
# shower_track-global, vertices-global, mc.
#
# bee_idx	evt	rse	OFF	ON	status
0	4	827-27-4	STM	STM	RESIDUAL, owner says NEUTRINO. Round-2 lead (doc 94 sec 0.3): the tell is at the ENTRY end -- dQ/dx 3.4 MIP at the boundary decaying to 1.05 by L=14 cm = two particles, one exiting. Every predicate in the tagger reads the STOP end. No prong at all (search_other_tracks found none), so nothing to key on. Textbook Bragg: body 1.08 MIP rising smoothly to 5.88 MIP at rr=0, ks1=0.022. Indistinguishable from a stopping muon by any charge/topology quantity available at STM time.
1	17	36-77-17	nu-candidate	nu-candidate	CONTROL, unchanged. Correctly not STM: save_stm_fit status 5 -- the eval ACCEPTED it and detect_proton saved it at ks1=0.047 (this corrects doc 93 sec 4).
2	22	966-2-22	STM	nu-candidate	RESCUED. 14.9 cm prong at 2.47 MIP, 90 deg to drift -- it reached the perpendicular skip, which is why the guard sits before it. PR layers appear in ON.
3	28	304-6-28	STM	nu-candidate	RESCUED. 25.9 cm prong at 2.22 MIP, straightness 0.719 -- the :2909 hot-curved skip. detect_proton HAD called a proton end; proton_muon_guard overruled it at ks1=0.035 vs its 0.040 bar. PR layers appear in ON.
4	12	707-18-12	STM	STM	CORRECT (owner 2026-09-02): a genuine STM, a muon coming in. Our tag is right; it was never a recovery target. Now the negative control for the round-2 entry-rise predicate (entry/body 1.26 vs 827-27-4 2.49). No prong. Body 0.85 MIP, end-5cm peak 4.32 MIP; detect_proton ran to block C and returned false.
5	31	146-60-31	STM	nu-candidate	RESCUED. 14.8 cm prong at 1.65 MIP, straightness 0.861 -- same :2909 skip; proton_muon_guard overruled a proton end at ks1=0.031. PR layers appear in ON.
6	10	921-29-10	TGM	TGM	unchanged. TGM exits before the STM fit, so no stm_fit layer either.
7	25	658-38-25	TGM	TGM	unchanged. Same.
8	64475	18255-1-64475	STM	nu-candidate	THE ONE DATA RELEASE -- 1 bundle of 34,827 over the full 3067-event A/B. main 340.4 cm; prong 20.0 cm at 2.38 MIP, straightness 0.894. OWNER CALL 2026-09-02: NEUTRINO -- so the guard has ZERO measured cost. FOR: TaggerCheckNeutrino's own vertex lands on the exact point the STM fit called the stop, with proton- and pion-typed stubs; numu_score +2.117; reco Enu 1092 MeV. AGAINST: 240 cm near-horizontal track entering the anode face; ragged, not monotone, end profile.
