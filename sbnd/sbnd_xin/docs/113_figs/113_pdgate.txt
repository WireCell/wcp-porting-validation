# doc 113: PDHD / PDVD imaging OFF gate attempt, 2026-09-19_11:01:32
abtest/run_events.sh d113pre img (LD_LIBRARY_PATH pinned to the pre-fix libWireCellImg.so 681378e33f0d) and d113post img (installed 90226edabb47): every event's imaging step returned rc=2 -- '[skip] no protodunehd-sp-dnnroi-frames-anode*.tar.bz2 in pdhd/work/<run>_<evt> (run run_nf_sp_dnnroi_evt.sh first)': the manifest events' SP frames are no longer on disk (docs 91/98/100-106 cleanup rounds).  The ab_compare PASS lines under snap/d113pre vs d113post compare the pre-existing archives copied from the work dirs and are VACUOUS; those two snapshot dirs hold no new run.  Regenerating the frames would write into existing work/ dirs (M13), so it was not done.
What stands instead: (1) the code path is guarded (MaskSlice.cxx: if (m_quiet_mask_window >= 0 && !m_masked_planes.empty())), C++ default -1; (2) prod_cfg_gate.py PASS 21/21 after the jsonnet edits -- the PDHD / PDVD / uBooNE / sim compiled configs are byte-identical, so no consumer carries the key; (3) the uBooNE chain has no MaskSlices node at all (its imaging is read from the prototype files: root/src/UbooneClusterSource.cxx); (4) the SBND runtime OFF gate (work-<s>-d113rep2 vs d102m, 32 events, same MaskSlices code, knob absent) -- see 113_off_gate.txt.
