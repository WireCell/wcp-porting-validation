# Left for the owner to settle (doc pr/116 bulk run)

1. **`EM_VERDICTS` has no entry for "the shower start / axis is wrong."**
   Carrying the literal grep phrase: evt167612, evt173819, evt174771, evt321767.
   Described in the same terms BEFORE the convention existed (wave 1, so no
   phrase to grep): evt67394 (seed is a 6.2 cm stem in the vertex fan, axis
   misses the body by 45 deg) and evt286655 (10 of 11 members at 128-150 deg to
   the shower's own axis; one OUT mark is the shower's own seed).
   `vertex-bad (undecidable)` means the *neutrino* vertex and does not cover it.
   The list is append-only and is the owner's; agents write the literal phrase
   `start/axis wrong` in the note so these can be grepped.

2. **evt179048 — verdict is rubric-dependent, left as recorded.**
   Recorded `vertex-bad (undecidable)` / `likely`.  The event is one straight
   311 cm MIP line with the nu vertex 62 cm from one end; shower 17012's own
   `cmp_div` reads *distance 0.0-247.9 cm, angle 0.0-4.7deg* -- 137.9 MeV spread
   collinearly over 249 cm, with the middle of the line correctly unowned.
   Verified independently: members reach r = 0.0 to 249.0 cm from the vertex,
   and the frame shows yellow member halos at BOTH ends of one line.
   Under correction 14 this should read `over-clustered`; it is left alone
   because changing a scanner's verdict is the owner's call.  Two OUT marks
   (17003, 17001) already carry the clustering claim either way.
