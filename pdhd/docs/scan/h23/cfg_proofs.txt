doc pdhd/23 -- compiled-config proofs (M6), generated 2026-09-12 06:29:31
instrument: docs/scan/h22/d22_cfg_keys.py -- parses the JSON STRUCTURE, because the
  compiled JSON writes '"key" : value' (space BEFORE the colon) and a grep finds nothing.
  Its limit, per doc pdhd/22 sec 1: it proves a key reached the CONFIG, never that the
  BINARY implements it.  All four keys here exist in d65f8165 (checked in the source).

== negative control: a file against itself ==
PROOF LINE: 0 added, 0 removed, 0 changed

== the flip: doc-22 production config -> doc-23 production config ==
  + bragg_wide_anchor_cm               8
  + bragg_wide_anchor_rise_min         1.3
  + bragg_wide_anchor_tail_max         0.8
  + topology_stop_evidence             True
PROOF LINE: 4 added, 0 removed, 0 changed

== the flipped FILE vs h23a's MEASURED TLA config -- production must run what was measured ==
PROOF LINE: 0 added, 0 removed, 0 changed

The confirmation arm h23conf was launched ONLY because both proofs above matched
exactly (4 added / 0 / 0, and 0 / 0 / 0); the launch was gated on those strings.
