# Cleanup round 2026-09-05 — pdhd + pdvd + sbnd_xin + ~/tmp

**Status: STAGED, NOTHING DELETED.** Every interlock passes and both dry runs
are clean. The two `CONFIRM=yes` steps are the owner's, as in every prior round.

## 0. Repro

```bash
cd /home/xqian/toolkit-dev/wcp-porting-img/pdhd/scripts/retire
python3 plan_20260905.py                 # all three trees; 27 PASS / 0 FAIL
./retire_20260905.sh all                 # dry run; counts must match the plan's
./sweep_tmp_20260905.sh                  # dry run for ~/tmp
# then, owner only:
CONFIRM=yes ./retire_20260905.sh all
CONFIRM=yes ./sweep_tmp_20260905.sh
```

## 1. What this round releases

| tree | before | staged | dirs | after |
|---|---|---|---|---|
| pdhd `work/` | 18 GiB | **2.05 GiB** | 12 | 16 GiB |
| pdvd `work/` | 58 GiB | **21.40 GiB** | 2489 | 37 GiB |
| sbnd_xin | 72 GiB | **10.36 GiB** | 65 | 62 GiB |
| `~/tmp` | 85 GiB | **34.25 GiB** | — | 51 GiB |
| **total** | | **68.1 GiB** | | |

`/home/xqian` currently has 494 G free; the round returns it to roughly 562 G.

## 2. Why pdhd releases so little, and why that is the right answer

pdhd looks like 30 GiB of scratch and is almost entirely record or substrate.

* `input_data_7p8_new_coh_grouping` and `input_data_14_old_coh_grouping` are
  **symlinks into `/nfs/data/1/xning/`** — another user's tree. Nothing in this
  round deletes through a symlink (INTERLOCK 8).
* The symlink census (698 links, both absolute and relative target forms) found
  a chain: **38 bare `029107_<N>` dirs ← `stm0` (270 links) ← `stmw`,
  `stmc250/1000/2000/4000`, `phdump*` (60 links each)**. The bare dirs are the
  *input substrate*, not old arms — the same shape as pdvd's `d27fresh`.
  Releasing one breaks the arms above it.
* `stm0` and `stmw` are the source of yesterday's 224-item STM hand scan, which
  resolves **30/30 events into both arms**. `stmw` is load-bearing beyond its
  numbers: the structural blind rests on `clustering-global` and
  `channel-deadarea` being byte-identical between the two arms.

`pdhd/scripts/retire/PROTECTED.txt` is new this round and states all of that,
sourced from the doc's own `§0` repro block and its "Not committed:" list
(`docs/stm-tagger-chain.md` lines 95 and 901) rather than from a name scan.

## 3. The interlocks, and the four things they caught

Nine interlocks, 27 PASS / 0 FAIL across three trees. Forked from
`pdvd/scripts/retire/plan_20260904.py`, **not** from the newest sbnd planner —
the relpath-against-`ROOT` alias defect that cost `work-dbg25a-ql` was only
fixed on the pdvd side, and `/nfs/data/1` is itself an alias for `/home/xqian`.

**1. INTERLOCK 6 caught a cross-repo citation on its first run.**
`029107_0_qlt` is a pdhd arm cited only by **pdvd** docs
(`qlmatch/02_pdvd-anode-time-consistency.md`, `03_pdhd-…`). A pdhd-only
blast-radius check would have released it — precisely the doc 100 §10 casualty,
where pdvd docs 28/31 cited an SBND arm an SBND-only check released. Every tree
is now resolved against the docs and scripts of all three. It also pulled
`d44prod` (doc 44 shipped its constant to PDVD production on 2026-09-05) and
`d31r6e2e` (18 citations), neither of which my name-based keep list had.

**2. The citation census counted its own output.** On the first run 11 of 12
pdhd dirs came back "cited" and direct grep showed none of them were: the census
scanned `scripts/retire/`, which now holds the tier file listing those very
dirs — protected because protected, the doc 91 defect exactly. Fixed by
excluding the retire machinery's own products, then given a **negative
control**: the fixed census still returns 2 and 3 for the two genuinely cited
arms and 0 for the rest.

**3. INTERLOCK 2 then failed, correctly.** Keeping the cited dirs left them
borrowing from retiring ones — kept `039252_5_d37off1` holds
`img-provenance.txt -> 039252_5_d34base`, which was in the release. Fixed with a
transitive closure to a fixed point; it pulled 5 more dirs into KEEP.

**4. INTERLOCK 9 caught two arms a live manifest still resolves into.**
`em_display`'s manifests name 420 arms and **10 exist** (the same number doc 100
recorded); `work-pr134-f086-{ncpi0,nuecc48}` were in the release set. The
decisive question is resolution, never "is it cited".

Also recorded before staging, because the post-state number is meaningless
otherwise: **0 pre-existing broken symlinks** in all three trees (196 in `~/tmp`).

## 4. What is kept, and on whose authority

| kept | why | source |
|---|---|---|
| pdvd `d27fresh` | 9215 inbound links; the documented default `src_tag` | `stage_{pr,ql}_tag.sh:7` (primary source, not a name-read) |
| pdvd `keep` | 0 inbound links but **never removed** | doc 24 lines 143, 234 |
| pdvd `d41prov` | 4398 inbound; the production PR chain doc 43 staged its 99 events from | doc 43 §6 |
| pdvd `d43p90c5`, `d42fit`, `d44sig`, `d44ref`, `d44prod` | back constants that shipped to PDVD production **on 2026-09-05** | doc 43 §8, doc 44 §0 repro |
| pdvd/sbnd `d45*` | **LIVE** — PID 1440435 is reading them now | `ps`, protected by PREFIX |
| sbnd `grp0825` | imaging substrate, 16 245 inbound links | doc 98 |
| sbnd `d97fv`/`d97fvpr2`, `vtx105-base`, `sent97` | production Q/L + PR, the vertex labels, the 31/31 witness | docs 98, 100 |
| pdhd `stm0`/`stmw`/`stmc*`, bare dirs, `ql_labels`, `stm_scan_labels` | the hand-scan record and its substrate | this doc §2, M13 |

An arm that backs a decision made yesterday is not superseded by a later arm at
a different operating point — the 08-31 `prod0825` refusal, applied to d41–d45.

**Protected by PREFIX, never by enumeration.** Doc 100 saw a live round create
seven new arm families between plan and confirm; `d45` is spelled as a prefix in
both pdvd and sbnd_xin so anything the peer creates before the owner confirms is
absorbed with no edit.

## 5. `~/tmp` — 34.25 GiB, records preserved in place

Doc 100's rule was to remove only whole `lib*/` dirs and build trees and never a
`*.log/.txt/.md/.json/.zip`, because a libsnap is regenerable from the commit
its doc records and a gate log is not. Measuring showed records and bulk are
cleanly separable here (`doc25gate` is 8.86 GiB of bulk and **0.00 GiB** of
records), so tier 3 deletes only non-record files and every log/txt/md/json/tsv
survives.

| tier | what | GiB |
|---|---|---|
| 1 | `cmrel1` build tree | 2.31 |
| 2 | pins of closed rounds (`pinlib`, `d31r6lib`, `d39r2_libpin`, `prod0901b-libsnap`, `d99r2-libsnap`, `d100regen-libsnap`) | 7.44 |
| 3 | bulk of closed-round scratch (`doc25gate`, `doc25r13`, `doc37`, `d39`, `doc25r12`, `d44sp`) | 24.43 |
| 4 | scratchpads of dead sessions | 0.08 |

**Liveness is from `ps`, never from age** — doc 100 nearly swept an 18 GiB
scratchpad that was untouched for two days and belonged to a running session.
The same one is live again now (PID 1314142, 17.46 GiB) and is kept, as are
PID 2711374's and this session's.

Checked before staging tier 2, because a missing `LD_LIBRARY_PATH` entry is
**silently ignored** and falls back to live `local/lib` (the M1 shape that bit
doc 100's regen driver): of the six pins, only `prod0901b-libsnap` is named by
anything, and that is a prior round's planner, not a runner. No runner breaks.

## 6. Owner calls — deliberately not staged

1. **`pdhd/l1sp_wf_v9`, 11 GiB**, 889 589 `.npz` across 34 runs, last written
   2026-05-22, **named by zero scripts or docs**. Zero-citation is exactly the
   trap `PROTECTED.txt` exists to prevent, so it is not swept on that basis. Its
   inputs (`input_data_*`, xning's tree) are still present and a v5 regen path
   exists next door (`l1sp_calib_v5`, `l1sp_v5_regen.log`), which suggests it is
   derived and rebuildable — but no v9 regen script was found. **Ask before
   dropping.**
2. **`/home/xqian/pdvd-frame-store`, 40 GiB.** Doc 24 line 46 calls it
   regenerate-only and line 92 says every one of the 120 `_keep` events has its
   complete original raw. If that raw still resolves this is a safe drop; if not
   it is a key input and stays.
3. **`d41_libpin` 7.78 G, `d42_libpin` 3.34 G, `d44_libpin` 2.22 G,
   `pdhdstm_libpin` 1.11 G = 14.45 GiB.** Doc 100's owner precedent was to drop
   pins and accept "re-readable, not re-runnable" — but that was for *closed*
   rounds, and these four closed yesterday. One decision, consequence stated
   once: dropping them leaves docs 41/42/44 and the STM hand scan readable and
   their arms intact, but re-running those arms would need a rebuild at the
   commit each doc records.

## 7. Out of scope, untouched

350 directories the arm grammar does not parse (267 pdvd `<run6>_light<flash>_<tag>`
light-reco dirs, 45 pdhd `029107_allpd*`, 38 sbnd non-`work-*`). They are
skipped by construction rather than by a rule, which is the conservative
direction.
